general {
    output_format = "i3bar"
    colors = true
    interval = 5
}

order += "tztime UTC"
order += "disk /"

tztime UTC {
    format = "%Y-%m-%d %H:%M:%S %Z"
    timezone = "UTC"
}

disk "/" {
    format = "%free"
}
