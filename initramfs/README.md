# My Small Custom Initramfs

## Why

Thought it would be fun to learn more about what an initramfs does

## Notes

This assumes you have `CONFIG_INITRAMFS_SOURCE` set to the location of `WDIR` in the makefile.
